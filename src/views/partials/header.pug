nav.nav
    .nav-links
        a(href="/").web-name 파주학당
        a.nav-menu Programs
        a.nav-menu About
        a.nav-menu Contact
        if !user
            a.nav-menu(href='/login') Login
            a.nav-menu(href='/signup') Signup
        else
            a(href='/logout') Logout
            if user.isAdmin
                a(href='/programs/upload' style="color: blue;") Upload
                a(href='/admin/dash' style="color: blue;") Admin

        //- a(href='#') MyPage
        //- if user.admin
            
            